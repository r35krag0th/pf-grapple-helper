stages:
  - deploy

deploy:
  stage: deploy
  image: node:22-alpine
  script:
    - npm ci
    - npm run build
    - npx wrangler pages deploy dist --project-name=pf-grapple-helper
  environment:
    name: production
    url: https://pf-grapple-helper.pages.dev
  only:
    - main
